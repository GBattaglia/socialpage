<nav class="navbar navbar-expand-lg navbar-light navbar-fixed-top bg-navbar-socialpage">
  <a class="navbar-brand" href="#">Navbar</a>
  <div style="float: right;margin-right: 5px !important">
    <ul class="navbar-nav list-unstyled">
      <li class="nav-item">
         <div class="dropdown">
          <button class="dropbtn">
            <% if user_signed_in? %>
              <%= current_user.name %> <%= current_user.surname %>
            <% end %>
          </button>
          <div class="dropdown-content">
            <%= link_to "il mio profilo",user_profile_path(id: current_user.id) %>
            <%= link_to "Modifica profilo",edit_user_registration_path %>
            <%= link_to('Logout', destroy_user_session_path, :method=>'delete')%>
          </div>
        </div>
      </li>
    </ul>
  </div>
</nav>